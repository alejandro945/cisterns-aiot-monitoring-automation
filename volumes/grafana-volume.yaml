apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-data
  namespace: metric
spec:
  storageClassName: local-storage
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/grafana"